%h1 Users
%form(method='post' action='/users')
  %table(border='1')
    %tr
      %th id
      %th first_name
      %th last_name
      %th initials
      %th google_plus_user_id
      %th is_admin
      %th is_student
      %th email
      %th seating_order
      %th github_username
    - (@users + [User.new]).each do |user|
      %tr
        %td
          - if user.id
            %input{:type => 'text', :name => "id_#{user.id}", :size => 2,
              :value => user.id }
          - else
            new
        %td
          %input{:type => 'text', :name => "first_name_#{user.id}",
            :size => 15,
            :value => user.first_name }
        %td
          %input{:type => 'text', :name => "last_name_#{user.id}",
            :size => 15,
            :value => user.last_name }
        %td
          %input{:type => 'text', :name => "initials_#{user.id}", :size => 2,
            :value => user.initials }
        %td
          %input{:type => 'text', :name => "google_plus_user_id_#{user.id}",
            :size => 30,
            :value => user.google_plus_user_id }
        %td
          %input{:type => 'text', :name => "is_admin_#{user.id}", :size => 1,
            :value => user.is_admin ? 't' : 'f' }
        %td
          %input{:type => 'text', :name => "is_student_#{user.id}", :size => 1,
            :value => user.is_student ? 't' : 'f' }
        %td
          %input{:type => 'text', :name => "email_#{user.id}",
            :size => 30,
            :value => user.email }
        %td
          %input{:type => 'text', :name => "seating_order_#{user.id}",
            :size => 2,
            :value => user.seating_order }
        %td
          %input{:type => 'text', :name => "github_username_#{user.id}",
            :value => user.github_username }
  %p
    %button Update
  
